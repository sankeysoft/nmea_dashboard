* Add "VMG to wind" and "VMG to waypoint" as automatically derived properties.
* Auto select default format when editing cells (based on previously used formats for the same dimension).
* Add "What's New" page (displays once on first launch of a new version) and release notes.