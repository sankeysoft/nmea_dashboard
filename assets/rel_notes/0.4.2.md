* Add "VMG to wind" and "VMG to waypoint" as calculated properties.
* Auto select default format when editing cells (based on previously used formats for the same dimension).
* Add "What's New" page (displays once on first launch of a new version).
* Add full release notes linked from the top level help page.